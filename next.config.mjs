/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['imagedelivery.net', 'https://myfirstframe.vercel.app', 'localhost:3000'],
    },
  };
  
  export default nextConfig;
  